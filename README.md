# miniapp-bootstrap

用于从 0 初始化小程序项目的 Codex/Agent Skill，默认采用 `uni-app + Vue 3 + TypeScript + Pinia` 前端、`NestJS + Prisma + MySQL` 后端、`pnpm workspace` monorepo。

它融合了两个参考 skill 的核心思想：一方面保留小程序项目生命周期、文档事实源、变更治理和验证流程；另一方面补充集中配置、统一响应、鉴权、SSE/长连接方案等全栈工程约束。

主文件见 [SKILL.md](SKILL.md)。
